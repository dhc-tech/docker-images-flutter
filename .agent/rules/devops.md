# DevOps Rules
- Always use `docker/metadata-action` for tagging and labeling images.
- Always implement `Dependabot Groups` for cleaner PR history.
- Ensure all Bash scripts inside workflows and Dockerfiles use `set -e` or `pipefail`.
